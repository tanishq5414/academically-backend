import { GATEWAY } from '../../constants';
import { isClientAuthenticated } from './client_authnetication';

export const AuthMiddlewares = {
  // [GATEWAY.MIDDLEWARE.AUTH_METHOD.CLIENT_AUTHENTICATION]: isClientAuthenticated,
};
